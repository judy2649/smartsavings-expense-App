This folder contains minimal placeholder Android files created by the assistant.

These are NOT a complete Flutter Android project. To generate a fully working Android project and Gradle config, run `flutter create .` on a machine with Flutter installed.

Place a real `google-services.json` here to configure Firebase for Android: `android/app/google-services.json`.
