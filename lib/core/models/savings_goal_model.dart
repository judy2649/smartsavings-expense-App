// Deprecated duplicate model file. Re-export the canonical implementation to
// avoid duplicate type definitions while preserving older import paths.
// Removed unused variable declaration
export 'savings_goal_impl_clean.dart';
