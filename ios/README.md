This folder contains minimal placeholder iOS files created by the assistant.

These are NOT a complete Flutter iOS project. To generate a fully working iOS project and Xcode files, run `flutter create .` on a macOS machine with Flutter installed.

Place a real `GoogleService-Info.plist` here to configure Firebase for iOS: `ios/Runner/GoogleService-Info.plist`.
